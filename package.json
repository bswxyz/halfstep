{
  "name": "halfstep",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Halfstep — one urban e-bike, stated plainly. Parable design showcase.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
